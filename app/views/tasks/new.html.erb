<h2>Form for a new task for <%= @group_task.name%>:</h2>

<%= form_for @task, url: group_task_tasks_path(@group_task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %> <br>
  <br>
  <%= f.submit "Add Task", { :class=>"button" } %>
<% end %>
